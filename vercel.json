{
    "installCommand": "npm install",
    "buildCommand": "npm run build -w @solvent/core && cd packages/dashboard && npm run build && cp -r dist ../..",
    "outputDirectory": "dist",
    "framework": null
}